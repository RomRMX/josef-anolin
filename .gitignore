node_modules/
dist/
.astro/
.vercel/
.DS_Store
# Claude Code per-machine worktree checkouts — never commit these (they get
# tracked as machine-specific gitlinks and break git on other machines).
.claude/worktrees/
.env
.env.*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel

# Local scratch doc
PUNCHLIST.html
