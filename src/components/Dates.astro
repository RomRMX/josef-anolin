---
// SHOWS section. No live dates yet, so it shows a placeholder. To restore a
// real schedule, add a dates array + list markup here (the previous
// implementation is in git history).
---

<div id="dates">
  <h2 class="section-h2 section-h2--lead">SHOWS</h2>
  <p class="dates-soon">Updates Coming Soon!</p>
</div>

<style>
  .dates-soon {
    margin: 0.25rem 0 0;
    font-family: var(--font-display), system-ui, sans-serif;
    font-size: clamp(1rem, 0.8rem + 0.8vw, 1.33rem);
    letter-spacing: 0.04em;
    color: var(--color-accent);
  }
</style>
