---
// TODO: replace these with real Instagram reel permalinks (e.g. https://www.instagram.com/reel/<id>/)
const reels: string[] = [];
---

<section id="videos" class="max-w-6xl mx-auto px-6 py-14 scroll-mt-24">
  <div class="section-rule mb-10"></div>
  <h2 class="font-[family-name:var(--font-display)] text-5xl md:text-6xl tracking-wide mb-8">
    VIDEOS
  </h2>

  {
    reels.length === 0 ? (
      <div class="rounded-lg border border-dashed border-white/15 p-10 text-center text-neutral-400">
        <p class="mb-4">Drop your Instagram reel URLs into <code class="text-[color:var(--color-accent)]">src/components/Videos.astro</code> to embed them here.</p>
        <a
          href="https://www.instagram.com/comedianjoe510/"
          target="_blank"
          rel="noopener noreferrer"
          class="inline-block px-5 py-2 border border-white/20 rounded-full text-sm tracking-wider hover:border-[color:var(--color-accent)] hover:text-[color:var(--color-accent)] transition-colors"
        >
          Watch on Instagram →
        </a>
      </div>
    ) : (
      <>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {reels.map((url) => (
            <blockquote
              class="instagram-media bg-neutral-900 rounded-lg overflow-hidden"
              data-instgrm-captioned
              data-instgrm-permalink={url}
              data-instgrm-version="14"
              style="margin: 0; min-width: 0; width: 100%;"
            />
          ))}
        </div>
        <script async src="https://www.instagram.com/embed.js" />
      </>
    )
  }
</section>
