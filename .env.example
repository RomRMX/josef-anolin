# Stripe — server-side secret key for the /api/checkout endpoint.
# Use a TEST key for local dev; set the live key in Vercel.
# Never commit a real key. Copy this file to `.env` and fill it in locally.
# Format: a test key starts with "sk_test_", a live key with "sk_live_".
STRIPE_SECRET_KEY=
